class Student {
	String name; // 实例变量
	int age; // 实例变量
	static String school; //静态变量
}

Student s1 = new Student();
s1.name = "哈机密"
s1.age = 18
s1.school = "第一中学" //这样如果s2.cshool也是第一中学，不过要Student s2 = new Student();也可以直接Student.school = "第一中学"，这就是静态变量，方法同理，注意静态变量前面有static

public class CS61BStudent { 
	static String university = "UC Berkeley"
		
} 
