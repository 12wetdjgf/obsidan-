吓哭了

在计算机程序的构造与解释里，这都是很难懂的概念，而且作业很难

试试java的for循环

int forXunHuan(int n){
	int res = 0;
	for (int i = 1; i <= n; i++){
		res += 1
	}
	return res;
}

哇，有点难写，这就是java吗

试试while

int whileXunHuan(int n) {
	int res = 0;
	int  i = 1;
	while(i <= n){
		res +=i;
		i++;
	}
}

代码就是可以练练手感，不过有ai力，写代码的机会越来越少

试试嵌套

String nestedForLoop{
	StringBuilder res = new StringBuilder();
	for (int i = 1;i <= n; i++);{
		for (int j; j<= n; j++);{
			res.append("(" + i +"," + j +"),");
			}
		}
		return res.toString();
}
// ai:String 像写在纸上的字，改一次就得重写一张,StringBuilder 像白板，可以一直往上追加内容


写代码太累了，先不写了

接下来是递归，没什么好说的，可以b站搜一下相关视频，代码还是很难写的

栈区可以是一个内存位