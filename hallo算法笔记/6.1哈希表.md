# 6.1 哈希表（Hash Table）

哈希表就是超级快的字典，通过**哈希函数**把 key 直接映射到存储位置，实现 O(1) 的查找。

## 哈希函数

```
key → 哈希函数 → 数组下标 → 存/取值
```

最简单的哈希函数：

```java
int hash(int key, int capacity) {
    return key % capacity;  // 取模
}
```

## 基本操作

```java
// Java 内置 HashMap，直接用！
Map<String, Integer> map = new HashMap<>();

map.put("alice", 99);          // 添加/修改
map.get("alice");               // 查询 → 99
map.remove("alice");            // 删除
map.containsKey("alice");       // 是否存在 → false
map.getOrDefault("bob", 0);    // 不存在返回默认值

// 遍历
for (Map.Entry<String, Integer> e : map.entrySet()) {
    System.out.println(e.getKey() + " → " + e.getValue());
}
for (String key : map.keySet()) { ... }
for (int val : map.values()) { ... }
```

## 哈希表 vs 数组 vs 链表

| 操作 | 数组 | 链表 | 哈希表 |
|------|------|------|-------|
| 查找 | O(n) | O(n) | O(1) |
| 插入 | O(n) | O(1) | O(1) |
| 删除 | O(n) | O(1) | O(1) |
| 空间 | O(n) | O(n) | O(n) |

## 典型应用

**两数之和（LeetCode 1，必刷！）**

```java
public int[] twoSum(int[] nums, int target) {
    Map<Integer, Integer> map = new HashMap<>();
    for (int i = 0; i < nums.length; i++) {
        int need = target - nums[i];
        if (map.containsKey(need))
            return new int[]{map.get(need), i};
        map.put(nums[i], i);
    }
    return new int[]{};
}
// O(n)，比暴力 O(n²) 快多了
```

**统计词频**

```java
Map<String, Integer> freq = new HashMap<>();
for (String word : words)
    freq.put(word, freq.getOrDefault(word, 0) + 1);
```

## 你来写！

```java
// 找出数组中第一个不重复的字符
public char firstUniqChar(String s) {
    // 提示：哈希表统计每个字符出现次数，再遍历一遍找第一个次数为1的
}
```

**口诀**：哈希表 = 神奇字典，key 进哈希函数变下标，O(1) 查找。
