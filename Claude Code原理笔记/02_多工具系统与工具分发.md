# Claude Code 原理笔记 · 第02篇：多工具系统与工具分发

> 💡 **核心洞察**：添加工具不需要改循环。只需要一个字典 + 一个查表。

---

## 🎯 问题：为什么不能只用Bash？

Bash虽然万能，但有问题：

```bash
# ❌ 问题1：不安全
cat /etc/passwd  # 可以读系统文件！

# ❌ 问题2：不可靠
cat huge_file.txt  # 输出可能被截断

# ❌ 问题3：不优雅
sed -i 's/old/new/g' file.txt  # 特殊字符容易出错
```

**解决方案**：用专门的工具替代bash的某些功能。

---

## 🔑 核心设计：工具分发（Dispatch）

不改循环，只改工具。怎么做？**用一个字典**。

```python
TOOL_HANDLERS = {
    "bash": run_bash,
    "read_file": run_read_file,
    "write_file": run_write_file,
    "edit_file": run_edit_file,
}

# 使用时：
handler = TOOL_HANDLERS[tool_name]
output = handler(**tool_input)
```

**这就是工具分发的全部秘密。** 一个字典，一个查表。

---

## 💻 核心代码片段

### 工具实现

```python
def safe_path(p: str) -> Path:
    """确保路径不会逃出工作目录（沙箱）"""
    path = (WORKDIR / p).resolve()
    if not path.is_relative_to(WORKDIR):
        raise ValueError(f"❌ 路径逃出工作目录: {p}")
    return path

def run_read_file(path: str) -> str:
    """读取文件"""
    try:
        content = safe_path(path).read_text(encoding='utf-8')
        return content
    except FileNotFoundError:
        return f"❌ 文件不存在: {path}"
    except Exception as e:
        return f"❌ 错误: {e}"

def run_write_file(path: str, content: str) -> str:
    """创建或覆盖文件"""
    try:
        file_path = safe_path(path)
        file_path.parent.mkdir(parents=True, exist_ok=True)
        file_path.write_text(content, encoding='utf-8')
        return f"✅ 文件已创建: {path}"
    except Exception as e:
        return f"❌ 错误: {e}"

def run_edit_file(path: str, old_text: str, new_text: str) -> str:
    """编辑文件（替换文本）"""
    try:
        file_path = safe_path(path)
        content = file_path.read_text(encoding='utf-8')
        
        if old_text not in content:
            return f"❌ 找不到要替换的文本"
        
        new_content = content.replace(old_text, new_text)
        file_path.write_text(new_content, encoding='utf-8')
        return f"✅ 文件已编辑: {path}"
    except Exception as e:
        return f"❌ 错误: {e}"
```

### 工具分发

```python
TOOL_HANDLERS = {
    "bash": lambda **kw: run_bash(kw["command"]),
    "read_file": lambda **kw: run_read_file(kw["path"]),
    "write_file": lambda **kw: run_write_file(kw["path"], kw["content"]),
    "edit_file": lambda **kw: run_edit_file(kw["path"], kw["old_text"], kw["new_text"]),
}

# 在循环中使用
handler = TOOL_HANDLERS.get(block.name)
if handler:
    output = handler(**block.input)
```

---

## 🎮 工具分发的魔法

### 之前（s01）：硬编码

```python
if block.name == "bash":
    output = run_bash(block.input["command"])
elif block.name == "read_file":
    output = run_read_file(block.input["path"])
elif block.name == "write_file":
    output = run_write_file(block.input["path"], block.input["content"])
```

**问题**：每添加一个工具，就要加一个elif。

### 现在（s02）：字典查表

```python
handler = TOOL_HANDLERS.get(block.name)
output = handler(**block.input)
```

**优点**：
- ✅ 添加工具只需要加一行
- ✅ 循环代码完全不变
- ✅ 清晰、优雅、可扩展

---

## 🔒 沙箱：路径安全

最关键的是 `safe_path()` 函数：

```python
def safe_path(p: str) -> Path:
    """确保路径不会逃出工作目录"""
    path = (WORKDIR / p).resolve()
    if not path.is_relative_to(WORKDIR):
        raise ValueError(f"路径逃出工作目录: {p}")
    return path
```

**为什么需要？**

```python
# ❌ 没有沙箱
read_file("../../../etc/passwd")  # 可以读系统文件！

# ✅ 有沙箱
read_file("../../../etc/passwd")  # 错误：路径逃出工作目录
```

---

## 📊 工具对比

| 工具 | 优点 | 缺点 | 何时用 |
|------|------|------|--------|
| **bash** | 万能 | 不安全、不可靠 | 真正需要时 |
| **read_file** | 安全、可靠 | 只能读 | 读文件 |
| **write_file** | 安全、可靠 | 只能写 | 创建文件 |
| **edit_file** | 精确、可靠 | 只能替换 | 编辑文件 |

---

## 🎓 你来试试！

### 挑战2：添加 `list_files` 工具

现在你要添加一个新工具 `list_files`，用来列出目录内容。

**任务**：
1. 在 `TOOLS` 列表中添加 `list_files` 的schema
2. 实现 `run_list_files()` 函数
3. 添加到 `TOOL_HANDLERS` 字典
4. 测试它

**提示**：
```python
def run_list_files(path: str) -> str:
    """列出目录内容"""
    try:
        dir_path = safe_path(path)
        if not dir_path.is_dir():
            return f"❌ 不是目录: {path}"
        
        files = list(dir_path.iterdir())
        # 返回格式化的文件列表
        # 提示：用 f.name 获取文件名，f.is_dir() 判断是否是目录
        
    except Exception as e:
        return f"❌ 错误: {e}"
```

**schema 应该是这样的**：
```python
{
    "name": "list_files",
    "description": "列出目录内容",
    "input_schema": {
        "type": "object",
        "properties": {
            "path": {"type": "string", "description": "目录路径"}
        },
        "required": ["path"]
    }
}
```

**测试**：
```python
agent_loop("列出当前目录的所有文件")
```

---

## 💬 口诀

> **"一个字典，一个查表，一个循环，无限工具。"**
>
> `TOOL_HANDLERS[tool_name](**tool_input)`

---

## 📝 总结

| 概念 | 说明 |
|------|------|
| **TOOL_HANDLERS** | 工具名 → 处理函数的映射 |
| **safe_path()** | 沙箱：防止路径逃出 |
| **lambda** | 匿名函数，用来包装参数 |
| **dispatch** | 根据名字查表调用函数 |

---

## 🚀 下一步

完成挑战后，我们进入 **s03：TodoWrite（任务规划）**

你会学到：
- 为什么Agent容易忘记任务
- 如何用TodoList让Agent有计划
- 如何用"提醒"让Agent保持专注

**准备好了吗？** 😎
