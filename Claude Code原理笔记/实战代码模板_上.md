# Claude Code 原理笔记 · 实战代码模板（上）

> 💻 **用途**：快速复制粘贴，开始你的Agent之旅。

---

## 🎯 模板1：最小化Agent（s01）

复制这个代码，修改任务，立即运行！

```python
from anthropic import Anthropic
import subprocess

client = Anthropic()

SYSTEM = """你是一个编程助手。
用户会给你任务，你需要用bash工具完成它们。"""

TOOLS = [
    {
        "name": "bash",
        "description": "在终端运行bash命令",
        "input_schema": {
            "type": "object",
            "properties": {
                "command": {"type": "string", "description": "bash命令"}
            },
            "required": ["command"]
        }
    }
]

def run_bash(command: str) -> str:
    """执行bash命令"""
    try:
        result = subprocess.run(
            command,
            shell=True,
            capture_output=True,
            text=True,
            timeout=10
        )
        return result.stdout + result.stderr
    except subprocess.TimeoutExpired:
        return "❌ 命令超时"
    except Exception as e:
        return f"❌ 错误: {e}"

def agent_loop(query: str):
    """Agent主循环"""
    messages = [{"role": "user", "content": query}]
    
    print(f"\n🤖 用户: {query}\n")
    
    while True:
        response = client.messages.create(
            model="claude-3-5-sonnet-20241022",
            system=SYSTEM,
            messages=messages,
            tools=TOOLS,
            max_tokens=8000,
        )
        
        messages.append({"role": "assistant", "content": response.content})
        
        if response.stop_reason != "tool_use":
            for block in response.content:
                if hasattr(block, "text"):
                    print(f"✅ Claude: {block.text}\n")
            return
        
        results = []
        for block in response.content:
            if block.type == "tool_use":
                print(f"🔧 执行: {block.input['command']}")
                output = run_bash(block.input["command"])
                print(f"📤 输出: {output[:100]}...\n" if len(output) > 100 else f"📤 输出: {output}\n")
                
                results.append({
                    "type": "tool_result",
                    "tool_use_id": block.id,
                    "content": output,
                })
        
        messages.append({"role": "user", "content": results})

if __name__ == "__main__":
    # 修改这里的任务
    agent_loop("创建一个文件叫 hello.py，内容是打印 'Hello, World!'")
```

---

## 🎯 模板2：多工具系统（s02）

添加了 `read_file`、`write_file`、`edit_file` 工具。

```python
from anthropic import Anthropic
from pathlib import Path
import subprocess

client = Anthropic()
WORKDIR = Path("./workspace")
WORKDIR.mkdir(exist_ok=True)

SYSTEM = """你是一个编程助手。
优先使用 read_file、write_file、edit_file，而不是bash。"""

TOOLS = [
    {
        "name": "bash",
        "description": "在终端运行bash命令",
        "input_schema": {
            "type": "object",
            "properties": {"command": {"type": "string"}},
            "required": ["command"]
        }
    },
    {
        "name": "read_file",
        "description": "读取文件内容",
        "input_schema": {
            "type": "object",
            "properties": {"path": {"type": "string"}},
            "required": ["path"]
        }
    },
    {
        "name": "write_file",
        "description": "创建或覆盖文件",
        "input_schema": {
            "type": "object",
            "properties": {
                "path": {"type": "string"},
                "content": {"type": "string"}
            },
            "required": ["path", "content"]
        }
    },
    {
        "name": "edit_file",
        "description": "编辑文件（替换文本）",
        "input_schema": {
            "type": "object",
            "properties": {
                "path": {"type": "string"},
                "old_text": {"type": "string"},
                "new_text": {"type": "string"}
            },
            "required": ["path", "old_text", "new_text"]
        }
    }
]

def safe_path(p: str) -> Path:
    """确保路径不会逃出工作目录"""
    path = (WORKDIR / p).resolve()
    if not path.is_relative_to(WORKDIR):
        raise ValueError(f"❌ 路径逃出工作目录: {p}")
    return path

def run_bash(command: str) -> str:
    try:
        result = subprocess.run(command, shell=True, capture_output=True, text=True, timeout=10, cwd=WORKDIR)
        return result.stdout + result.stderr
    except subprocess.TimeoutExpired:
        return "❌ 命令超时"
    except Exception as e:
        return f"❌ 错误: {e}"

def run_read_file(path: str) -> str:
    try:
        return safe_path(path).read_text(encoding='utf-8')
    except FileNotFoundError:
        return f"❌ 文件不存在: {path}"
    except Exception as e:
        return f"❌ 错误: {e}"

def run_write_file(path: str, content: str) -> str:
    try:
        file_path = safe_path(path)
        file_path.parent.mkdir(parents=True, exist_ok=True)
        file_path.write_text(content, encoding='utf-8')
        return f"✅ 文件已创建: {path}"
    except Exception as e:
        return f"❌ 错误: {e}"

def run_edit_file(path: str, old_text: str, new_text: str) -> str:
    try:
        file_path = safe_path(path)
        content = file_path.read_text(encoding='utf-8')
        if old_text not in content:
            return f"❌ 找不到要替换的文本"
        new_content = content.replace(old_text, new_text)
        file_path.write_text(new_content, encoding='utf-8')
        return f"✅ 文件已编辑: {path}"
    except Exception as e:
        return f"❌ 错误: {e}"

TOOL_HANDLERS = {
    "bash": lambda **kw: run_bash(kw["command"]),
    "read_file": lambda **kw: run_read_file(kw["path"]),
    "write_file": lambda **kw: run_write_file(kw["path"], kw["content"]),
    "edit_file": lambda **kw: run_edit_file(kw["path"], kw["old_text"], kw["new_text"]),
}

def agent_loop(query: str):
    messages = [{"role": "user", "content": query}]
    print(f"\n🤖 用户: {query}\n")
    
    while True:
        response = client.messages.create(
            model="claude-3-5-sonnet-20241022",
            system=SYSTEM,
            messages=messages,
            tools=TOOLS,
            max_tokens=8000,
        )
        
        messages.append({"role": "assistant", "content": response.content})
        
        if response.stop_reason != "tool_use":
            for block in response.content:
                if hasattr(block, "text"):
                    print(f"✅ Claude: {block.text}\n")
            return
        
        results = []
        for block in response.content:
            if block.type == "tool_use":
                handler = TOOL_HANDLERS.get(block.name)
                output = handler(**block.input) if handler else "❌ 未知工具"
                print(f"🔧 {block.name}: {output[:80]}...\n" if len(output) > 80 else f"🔧 {block.name}: {output}\n")
                
                results.append({
                    "type": "tool_result",
                    "tool_use_id": block.id,
                    "content": output,
                })
        
        messages.append({"role": "user", "content": results})

if __name__ == "__main__":
    agent_loop("创建一个文件叫 greet.py，内容是一个 greet(name) 函数")
```

---

## 🎮 快速测试

### 测试s01

```python
if __name__ == "__main__":
    agent_loop("创建一个文件叫 hello.py，内容是打印 'Hello, World!'")
```

### 测试s02

```python
if __name__ == "__main__":
    agent_loop("创建一个文件叫 greet.py，内容是一个 greet(name) 函数")
    agent_loop("编辑 greet.py，给函数添加一个docstring")
    agent_loop("读取 greet.py 验证编辑成功")
```

---

## 🔧 常见修改

### 修改模型

```python
# 改这一行
model="claude-3-5-sonnet-20241022"

# 改成
model="claude-opus-4-1"  # 或其他模型
```

### 修改工作目录

```python
# 改这一行
WORKDIR = Path("./workspace")

# 改成
WORKDIR = Path("C:/Users/14936/my_workspace")
```

### 修改系统提示

```python
# 改这一行
SYSTEM = """你是一个编程助手。..."""

# 改成你自己的提示
SYSTEM = """你是一个数据分析助手。..."""
```

---

## 💡 调试技巧

### 打印消息历史

```python
import json
print(json.dumps(messages, indent=2, ensure_ascii=False))
```

### 打印工具调用

```python
print(f"工具: {block.name}")
print(f"参数: {json.dumps(block.input, indent=2, ensure_ascii=False)}")
```

### 打印响应

```python
print(f"stop_reason: {response.stop_reason}")
print(f"内容: {response.content}")
```

---

## 🚀 下一步

查看 **实战代码模板（下）** 了解s03和s04的模板。

**准备好了吗？** 😎
